name: car_damage_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - flask
      - flask-cors
      - ultralytics
      - opencv-python
      - torch
      - torchvision
